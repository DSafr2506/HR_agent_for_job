# Career Advisor Agent

Асинхронный карьерный агент с векторным хранилищем – проект, который анализирует диалог между карьерным консультантом и пользователем с помощью LLM-модели и предоставляет персональные рекомендации. Проект использует современную архитектуру с применением паттернов проектирования и векторное хранилище для семантического поиска.

##  Основные возможности

- **Векторное хранилище** - все данные хранятся в векторном формате (ChromaDB)
  - Профили пользователей
  - Ресурсы (курсы, статьи, проекты, соревнования)
  - Диалоги
  - Вакансии

- **Семантический поиск** - поиск ресурсов по смыслу, а не только по ключевым словам

- **Полностью асинхронная архитектура** - все операции выполняются асинхронно для максимальной производительности

- **LLM через SciBox API** - анализ диалога и генерация рекомендаций

- **Параллельный поиск ресурсов** - поиск по Coursera, Stepik, Habr Career, GitHub, Kaggle

## 📋 Требования

- Python 3.9+ (рекомендуется 3.10 или выше)
- ChromaDB для векторного хранилища
- Sentence Transformers для эмбеддингов

##  Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd career_advisor_agent
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл и укажите SCIBOX_API_KEY
```

4. Запустите API сервер:
```bash
python api_server.py
```

API будет доступен по адресу: `http://localhost:8000`
- Документация API: `http://localhost:8000/docs`
- Health check: `http://localhost:8000/health`

##  Использование

### API Endpoints

#### 1. Анализ диалога
```bash
POST /api/analyze-conversation
Content-Type: application/json

{
    "messages": [
        {"role": "user", "message": "Хочу стать Python разработчиком"},
        {"role": "assistant", "message": "Расскажите о вашем опыте"},
        {"role": "user", "message": "Изучаю Python 6 месяцев, знаю основы"}
    ],
    "user_id": "user123"
}
```

#### 2. Извлечение профиля пользователя
```bash
POST /api/get-user-profile
Content-Type: application/json

{
    "dialog_text": "Пользователь: Хочу стать Python разработчиком\nКонсультант: Расскажите о вашем опыте",
    "user_id": "user123"
}
```

#### 3. Поиск ресурсов
```bash
POST /api/find-resources
Content-Type: application/json

{
    "skills": ["Python", "Machine Learning", "Django"]
}
```

#### 4. Анализ соответствия вакансии
```bash
POST /api/match-vacancy
Content-Type: application/json

{
    "user_profile": {
        "skills": ["Python", "Django"],
        "experience": "2 года разработки",
        "education": "Высшее техническое"
    },
    "vacancy_info": "Требуется Python разработчик с опытом 3+ года, знание Django, PostgreSQL"
}
```

#### 5. Получение карьерных советов
```bash
POST /api/career-advice
Content-Type: application/json

{
    "user_goals": "Стать senior Python разработчиком",
    "current_skills": ["Python", "Django"],
    "challenges": "Недостаток опыта в тестировании"
}
```

### Пример использования (Python)

```python
import requests

client = requests.Session()
base_url = "http://localhost:8000"

# Анализ диалога
messages = [
    {"role": "user", "message": "Хочу стать Python разработчиком"},
    {"role": "assistant", "message": "Расскажите о вашем опыте"},
    {"role": "user", "message": "Изучаю Python 6 месяцев"}
]

response = client.post(
    f"{base_url}/api/analyze-conversation",
    json={"messages": messages, "user_id": "user123"}
)
result = response.json()
print(result["recommendations"])
```

### Пример использования (JavaScript)

```javascript
// Анализ диалога
const analyzeConversation = async (messages, userId) => {
    const response = await fetch('/api/analyze-conversation', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({ messages, user_id: userId })
    });
    
    const result = await response.json();
    return result;
};

// Использование
const messages = [
    { role: "user", message: "Хочу стать Python разработчиком" },
    { role: "assistant", message: "Расскажите о вашем опыте" }
];

analyzeConversation(messages, "user123")
    .then(result => {
        console.log('Рекомендации:', result.recommendations);
    })
    .catch(error => {
        console.error('Ошибка:', error);
    });
```

##  Архитектура

Проект использует чистую архитектуру с разделением на слои:

### Domain Layer (Доменный слой)
**Путь:** `domain/`

Содержит бизнес-логику и доменные сущности:
- `entities.py` - Доменные сущности (UserProfile, Resource, Conversation, Vacancy)
- `repositories.py` - Интерфейсы репозиториев (Repository Pattern)

### Infrastructure Layer (Инфраструктурный слой)
**Путь:** `infrastructure/`

Реализация технических деталей:
- `vector_store/` - Векторное хранилище (ChromaDB)
- `repositories/` - Реализации репозиториев с векторным хранилищем
- `llm/` - Сервис для работы с LLM
- `embeddings/` - Сервис для создания эмбеддингов
- `searcher/` - Поиск ресурсов в интернете
- `di/` - Контейнер зависимостей (Dependency Injection)

### Application Layer (Слой приложения)
**Путь:** `application/`

Бизнес-логика приложения:
- `services/` - Сервисы приложения (CareerService)

### API Layer (Слой API)
**Путь:** `api_server.py`

REST API для взаимодействия с внешними системами

## 🔍 Векторное хранилище

### Технология
- **ChromaDB** - векторная база данных для хранения эмбеддингов
- **Sentence Transformers** - модель для создания эмбеддингов (multilingual)

### Что хранится в векторном формате:

1. **Профили пользователей** (`user_profiles`)
   - Цели, навыки, опыт, проблемы
   - Поиск похожих профилей для рекомендаций

2. **Ресурсы** (`resources`)
   - Курсы, статьи, проекты, соревнования
   - Семантический поиск по навыкам

3. **Диалоги** (`conversations`)
   - История общения пользователя с консультантом
   - Поиск похожих ситуаций

4. **Вакансии** (`vacancies`)
   - Описания вакансий и требования
   - Подбор вакансий под профиль пользователя

### Преимущества векторного хранилища:

- **Семантический поиск** - поиск по смыслу, а не только по ключевым словам
- **Масштабируемость** - эффективный поиск даже при больших объемах данных
- **Персонализация** - нахождение релевантных ресурсов на основе профиля пользователя
- **Гибкость** - легко добавлять новые типы данных

##  Паттерны проектирования

### Repository Pattern
Абстракция доступа к данным. Все операции с данными выполняются через интерфейсы репозиториев.

### Factory Pattern
Создание объектов векторного хранилища через фабрику.

### Dependency Injection
Управление зависимостями через контейнер (DIContainer).

### Strategy Pattern
Различные реализации векторного хранилища (ChromaDB, FAISS - в планах).

##  Конфигурация

Все настройки через переменные окружения (`.env`):

```env
# API Keys
SCIBOX_API_KEY=your-scibox-api-key-here
SCIBOX_API_URL=https://llm.t1v.scibox.tech/v1
SCIBOX_MODEL=Qwen2.5-72B-Instruct-AWQ

# GitHub Token (опционально)
GITHUB_TOKEN=your-github-token-here

# Vector Store Settings
VECTOR_STORE_TYPE=chroma
VECTOR_STORE_PATH=./vector_store
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

# Application Settings
MAX_HISTORY_LENGTH=10
REQUEST_TIMEOUT=60
MAX_RESOURCES_PER_CATEGORY=10

# Logging
LOG_LEVEL=INFO
```

##  Тестирование

Запустите тестовый клиент:
```bash
python test_api_client.py
```

Убедитесь, что API сервер запущен перед тестированием.

##  Структура проекта

```
career_advisor_agent/
├── domain/                    # Доменный слой
│   ├── entities.py           # Сущности
│   └── repositories.py       # Интерфейсы репозиториев
├── infrastructure/           # Инфраструктурный слой
│   ├── vector_store/        # Векторное хранилище
│   ├── repositories/         # Реализации репозиториев
│   ├── llm/                 # LLM сервис
│   ├── embeddings/          # Сервис эмбеддингов
│   ├── searcher/            # Поиск ресурсов
│   └── di/                  # Dependency Injection
├── application/             # Слой приложения
│   └── services/            # Сервисы приложения
├── config/                  # Конфигурация
│   └── settings.py          # Настройки
├── api_server.py            # API сервер
├── test_api_client.py       # Тестовый клиент
└── requirements.txt         # Зависимости
```

##  Поток данных

```
API Request
    ↓
CareerService (Application Layer)
    ↓
Repositories (Infrastructure Layer)
    ↓
Vector Store (ChromaDB)
    ↓
Embeddings (Sentence Transformers)
    ↓
Response
```

##  Обработка ошибок

Проект использует кастомные исключения:
- `CareerAdvisorException` - базовое исключение
- `VectorStoreException` - ошибки векторного хранилища
- `LLMServiceException` - ошибки LLM сервиса
- `RepositoryException` - ошибки репозиториев

##  Производительность

- **Семантический поиск**: Находит релевантные ресурсы даже при неточных запросах
- **Кэширование**: Найденные ресурсы сохраняются и переиспользуются
- **Масштабируемость**: Векторное хранилище эффективно работает с большими объемами данных
- **Параллелизм**: Асинхронные операции для максимальной производительности

##  Вклад

Проект использует чистую архитектуру, что упрощает добавление новых функций:

- **Новые типы ресурсов** - добавьте в `domain/entities.py`
- **Новые источники данных** - реализуйте репозиторий в `infrastructure/repositories/`
- **Новые API endpoints** - добавьте в `api_server.py`
- **Новые типы векторных хранилищ** - реализуйте в `infrastructure/vector_store/`

##  Будущие улучшения

- [ ] Поддержка FAISS хранилища
- [ ] Индексация для ускорения поиска
- [ ] Метрики и мониторинг
- [ ] Тесты для всех компонентов
- [ ] WebSocket поддержка для real-time обновлений


